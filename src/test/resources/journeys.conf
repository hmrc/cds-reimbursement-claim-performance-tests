# Copyright 2023 HM Revenue & Customs
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# Configure here your journeys. A journey is a sequence of requests at a certain load.

journeys {

  # Give a name to the journey.


    Rejected-Goods-Single-MRN-journey = {

            # The description will appear in the test report. Use something meaningful
            description = "Rejected Goods Single MRN journey"

            # The load is in journeys per second. Put here the load you are going to have at the peak.
            # There is no need to put a higher value at this point. Use prerftest.loadPercentage in application.conf instead
            load = 1.0

            # This points to a csv file with the data you need to inject in the journey. [More here](https://github.com/hmrc/performance-test-runner#step-4-configure-the-user-feeder)
            feeder = data/entryNumber.csv

            # The parts your journey is made of. A part is made one or more requests.
            parts = [
            Rejected-Goods-Single-MRN-journey
          ]
        }

    Rejected-Goods-Multiple-MRN-journey = {

            # The description will appear in the test report. Use something meaningful
            description = "Rejected Goods Multiple MRN journey"

            # The load is in journeys per second. Put here the load you are going to have at the peak.
            # There is no need to put a higher value at this point. Use prerftest.loadPercentage in application.conf instead
            load = 1.0

            # This points to a csv file with the data you need to inject in the journey. [More here](https://github.com/hmrc/performance-test-runner#step-4-configure-the-user-feeder)
            feeder = data/entryNumber.csv

            # The parts your journey is made of. A part is made one or more requests.
            parts = [
            Rejected-Goods-Multiple-MRN-journey
          ]
        }

    Rejected-Goods-Scheduled-MRN-journey = {

           # The description will appear in the test report. Use something meaningful
           description = "Rejected Goods Scheduled MRN journey"

           # The load is in journeys per second. Put here the load you are going to have at the peak.
           # There is no need to put a higher value at this point. Use prerftest.loadPercentage in application.conf instead
           load = 0.1

           # This points to a csv file with the data you need to inject in the journey. [More here](https://github.com/hmrc/performance-test-runner#step-4-configure-the-user-feeder)
           feeder = data/entryNumber.csv

           # The parts your journey is made of. A part is made one or more requests.
           parts = [
           Rejected-Goods-Scheduled-MRN-journey
         ]
       }

    Securities-Single-MRN-BOD4-journey = {

          # The description will appear in the test report. Use something meaningful
          description = "Securities Single MRN with BOD4 journey"

          # The load is in journeys per second. Put here the load you are going to have at the peak.
          # There is no need to put a higher value at this point. Use prerftest.loadPercentage in application.conf instead
          load = 0.1

          # This points to a csv file with the data you need to inject in the journey. [More here](https://github.com/hmrc/performance-test-runner#step-4-configure-the-user-feeder)
          feeder = data/entryNumber.csv

          # The parts your journey is made of. A part is made one or more requests.
          parts = [
          Securities-Single-MRN-BOD4-journey
         ]
       }

    Securities-Single-MRN-BOD3-journey = {

         # The description will appear in the test report. Use something meaningful
         description = "Securities Single MRN with BOD3 journey"

         # The load is in journeys per second. Put here the load you are going to have at the peak.
         # There is no need to put a higher value at this point. Use prerftest.loadPercentage in application.conf instead
         load = 0.1

         # This points to a csv file with the data you need to inject in the journey. [More here](https://github.com/hmrc/performance-test-runner#step-4-configure-the-user-feeder)
         feeder = data/entryNumber.csv

         # The parts your journey is made of. A part is made one or more requests.
         parts = [
         Securities-Single-MRN-BOD3-journey
        ]
      }
    Securities-Single-MRN-TA-journey = {

        # The description will appear in the test report. Use something meaningful
        description = "Securities Single MRN with Temporary Admissions journey"

        # The load is in journeys per second. Put here the load you are going to have at the peak.
        # There is no need to put a higher value at this point. Use prerftest.loadPercentage in application.conf instead
        load = 0.1

        # This points to a csv file with the data you need to inject in the journey. [More here](https://github.com/hmrc/performance-test-runner#step-4-configure-the-user-feeder)
        feeder = data/entryNumber.csv

        # The parts your journey is made of. A part is made one or more requests.
        parts = [
        Securities-Single-MRN-TA-journey
       ]
     }

    OverPayments-Single-MRN-journey = {

                  # The description will appear in the test report. Use something meaningful
                  description = "Overpayments Single Movement reference number journey"

                  # The load is in journeys per second. Put here the load you are going to have at the peak.
                  # There is no need to put a higher value at this point. Use prerftest.loadPercentage in application.conf instead
                  load = 0.1

                  # This points to a csv file with the data you need to inject in the journey. [More here](https://github.com/hmrc/performance-test-runner#step-4-configure-the-user-feeder)
                  feeder = data/entryNumber.csv

                  # The parts your journey is made of. A part is made one or more requests.
                  parts = [
                    OverPayments-Single-MRN-journey
                  ]
                }

    OverPayments-Bulk-Multiple-V2-MRN-journey = {

                      # The description will appear in the test report. Use something meaningful
                      description = "Overpayments Bulk Multiple V2 Movement reference number journey"

                      # The load is in journeys per second. Put here the load you are going to have at the peak.
                      # There is no need to put a higher value at this point. Use prerftest.loadPercentage in application.conf instead
                      load = 0.1

                      # This points to a csv file with the data you need to inject in the journey. [More here](https://github.com/hmrc/performance-test-runner#step-4-configure-the-user-feeder)
                      feeder = data/entryNumber.csv

                      # The parts your journey is made of. A part is made one or more requests.
                      parts = [
                        OverPayments-Bulk-Multiple-V2-MRN-journey
                      ]
                    }

    OverPayments-Bulk-Scheduled-V2-MRN-journey = {

                          # The description will appear in the test report. Use something meaningful
                          description = "Overpayments Bulk Scheduled V2 Movement reference number journey"

                          # The load is in journeys per second. Put here the load you are going to have at the peak.
                          # There is no need to put a higher value at this point. Use prerftest.loadPercentage in application.conf instead
                          load = 0.1

                          # This points to a csv file with the data you need to inject in the journey. [More here](https://github.com/hmrc/performance-test-runner#step-4-configure-the-user-feeder)
                          feeder = data/entryNumber.csv

                          # The parts your journey is made of. A part is made one or more requests.
                          parts = [
                            OverPayments-Bulk-Scheduled-V2-MRN-journey
                          ]
                        }

    }


# Default behaviour is to run all journeys. If that is not what you need you can specify the list of journeys to run
# journeysToRun = [
# Entry-number-journey#OverPayments-Single-MRN-journey
#   OverPayments-Bulk-Multiple-V2-MRN-journey
#   OverPayments-Bulk-Scheduled-V2-MRN-journey
#   Rejected-Goods-Single-MRN-journey
#   Rejected-Goods-Multiple-MRN-journey
#   Rejected-Goods-Scheduled-MRN-journey
#  Securities-Single-MRN-BOD4-journey
#   Securities-Single-MRN-BOD3-journey
#  Securities-Single-MRN-TA-journey
# ]

# You can specify the same list of journeys via environment variables:
# journeysToRun.0 = hello-world-1
# journeysToRun.1 = hello-world-3
